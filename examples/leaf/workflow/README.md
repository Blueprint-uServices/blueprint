# Leaf Application - Workflow Spec

This contains the workflow spec for the simple "leaf" application.

The workflow spec defines two services, called Leaf and NonLeaf.  NonLeaf makes RPC calls to Leaf.